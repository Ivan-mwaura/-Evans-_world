#include <stdio.h>
#include <math.h>
//height and width of the rectangle in centimeters

int main()

 {
    system("color 0a");
 	float length,width,area,perimeter;
 	length = 7;
 	width = 5;

 	printf("length -> ");
 	printf("width -> ");

 	area = length*width;
 	perimeter = (2*(length + width));
 	//print the output of area and perimeter

 	printf("\n The area is %f centimeters\n",area);
 	printf("\n The perimeter is %f centimeters\n",perimeter),

 	 system("PAUSE");




 }
